name: Deploy to Development

on:
  push:
    branches:
      - main

jobs:
  deploy:
    name: Deploy to dev
    runs-on: self-hosted

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Clean workspace
        run: |
          rm -rf node_modules
          
      - name: Sync code to deployment directory
        run: |
          sudo rsync -av --delete \
            $GITHUB_WORKSPACE/ \
            /home/ubuntu/docker/admin/app/code/

      - name: Deploy with Docker Compose
        run: |
          set -euo pipefail

          cd /home/ubuntu/docker/admin/app

          echo "Building and starting containers"
          docker compose build --no-cache
          docker compose up -d

          echo "Restarting proxy_server"
          docker restart proxy_server || echo "proxy_server not running"

          echo "Deployment finished"
          echo "Website live at https://adminpanel.resq-qa.com/"
