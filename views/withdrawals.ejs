<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Withdrawals | ResQ Admin</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/dashboard.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>
        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
            gap: 24px;
            margin-bottom: 32px;
        }

        .stat-card {
            background: white;
            padding: 24px;
            border-radius: 12px;
            border: 1px solid #e2e8f0;
            box-shadow: 0 1px 2px rgba(0, 0, 0, 0.05);
        }

        .stat-label {
            color: #64748b;
            font-size: 0.875rem;
            font-weight: 500;
            margin-bottom: 8px;
        }

        .stat-value {
            font-size: 1.5rem;
            font-weight: 700;
            color: #1e293b;
        }

        .stat-subtext {
            color: #94a3b8;
            font-size: 0.875rem;
            margin-top: 4px;
        }

        .status-tabs {
            display: flex;
            gap: 4px;
            margin-bottom: 24px;
            border-bottom: 1px solid #e2e8f0;
            padding-bottom: 12px;
            overflow-x: auto;
        }

        .status-tab {
            padding: 8px 16px;
            border-radius: 6px;
            color: #64748b;
            font-weight: 500;
            text-decoration: none;
            cursor: pointer;
            white-space: nowrap;
            transition: all 0.2s;
        }

        .status-tab:hover {
            background: #f1f5f9;
            color: #334155;
        }

        .status-tab.active {
            background: #e0f2fe;
            color: #0284c7;
        }

        .badge.status-pending {
            background: #fff7ed;
            color: #c2410c;
            border: 1px solid #ffedd5;
        }

        .badge.status-processing {
            background: #eff6ff;
            color: #1d4ed8;
            border: 1px solid #dbeafe;
        }

        .badge.status-completed {
            background: #f0fdf4;
            color: #15803d;
            border: 1px solid #dcfce7;
        }

        .badge.status-rejected {
            background: #fef2f2;
            color: #b91c1c;
            border: 1px solid #fee2e2;
        }

        .driver-info {
            display: flex;
            align-items: center;
            gap: 12px;
        }

        .action-btn {
            padding: 6px 12px;
            border-radius: 6px;
            font-size: 0.875rem;
            font-weight: 500;
            cursor: pointer;
            border: none;
            transition: background 0.2s;
        }

        .btn-approve {
            background: #f0fdf4;
            color: #15803d;
            border: 1px solid #dcfce7;
        }

        .btn-approve:hover {
            background: #dcfce7;
        }

        .btn-reject {
            background: #fef2f2;
            color: #b91c1c;
            border: 1px solid #fee2e2;
        }

        .btn-reject:hover {
            background: #fee2e2;
        }

        .btn-complete {
            background: #eff6ff;
            color: #1d4ed8;
            border: 1px solid #dbeafe;
        }

        .btn-complete:hover {
            background: #dbeafe;
        }

        /* Modal Styles */
        .modal-overlay {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: rgba(0, 0, 0, 0.5);
            display: flex;
            align-items: center;
            justify-content: center;
            z-index: 1000;
            opacity: 0;
            visibility: hidden;
            transition: all 0.2s;
        }

        .modal-overlay.active {
            opacity: 1;
            visibility: visible;
        }

        .modal {
            background: white;
            padding: 24px;
            border-radius: 12px;
            width: 100%;
            max-width: 400px;
            transform: translateY(20px);
            transition: all 0.2s;
        }

        .modal-overlay.active .modal {
            transform: translateY(0);
        }

        .modal-title {
            font-size: 1.25rem;
            font-weight: 700;
            margin-bottom: 16px;
            color: #1e293b;
        }

        .modal-actions {
            display: flex;
            justify-content: flex-end;
            gap: 12px;
            margin-top: 24px;
        }

        textarea.form-control {
            width: 100%;
            padding: 12px;
            border: 1px solid #e2e8f0;
            border-radius: 8px;
            resize: vertical;
            min-height: 100px;
            font-family: inherit;
        }

        /* Mobile Responsiveness */
        @media (max-width: 1024px) {
            .stats-grid {
                grid-template-columns: 1fr;
                gap: 16px;
                margin-bottom: 24px;
            }

            .stat-card {
                padding: 16px;
            }

            .status-tabs {
                padding-bottom: 8px;
                margin-bottom: 16px;
            }

            /* Card View for Table */
            .table-container table,
            .table-container thead,
            .table-container tbody,
            .table-container th,
            .table-container td,
            .table-container tr {
                display: block;
            }

            .table-container thead tr {
                position: absolute;
                top: -9999px;
                left: -9999px;
            }

            .table-container tr {
                background: white;
                border: 1px solid #e2e8f0;
                border-radius: 12px;
                margin-bottom: 16px;
                padding: 16px;
                box-shadow: 0 1px 2px rgba(0, 0, 0, 0.05);
            }

            .table-container td {
                border: none;
                padding: 10px 0;
                position: relative;
                display: flex;
                justify-content: space-between;
                align-items: flex-start;
                /* Align top for multi-line text */
                text-align: right;
                border-bottom: 1px solid #f8fafc;
            }

            .table-container td:last-child {
                border-bottom: none;
                margin-top: 12px;
                padding-top: 12px;
                border-top: 1px solid #f1f5f9;
                justify-content: flex-end;
            }

            .table-container td:before {
                content: attr(data-label);
                font-weight: 600;
                color: #64748b;
                font-size: 0.875rem;
                margin-right: 16px;
                text-align: left;
                flex-shrink: 0;
                /* Prevent label from shrinking */
                width: 40%;
                /* Fixed width for labels */
            }

            .driver-info,
            .fw-bold,
            .text-muted,
            .small {
                text-align: right;
                word-break: break-word;
                /* Prevent overflow */
            }



            .driver-info {
                flex-direction: row-reverse;
                width: 100%;
                justify-content: flex-start;
            }

            .table-container td>div:not(.driver-info),
            .table-container td>span {
                max-width: 100%;
                /* Allow full width */
                margin-left: 0;
                text-align: left;
                /* Align text left for better readability */
            }

            /* Fix Bank Details alignment in card view */
            .table-container td[data-label="Bank Details"] {
                flex-direction: column;
                align-items: flex-start;
                gap: 8px;
            }

            .table-container td[data-label="Bank Details"]:before {
                margin-bottom: 4px;
                width: 100%;
            }

            .table-container td[data-label="Bank Details"]>div {
                width: 100%;
                text-align: left;
            }

            .actions-cell {
                width: 100%;
                justify-content: flex-end;
                gap: 8px;
                flex-wrap: wrap;
                /* Allow buttons to wrap */
            }

            .table-container td:last-child:before {
                display: none;
            }

            .action-btn {
                padding: 10px 16px;
                flex: 1;
                min-width: 100px;
                /* Ensure clickability */
                text-align: center;
            }
        }

        /* Responsive Table Styles - High Specificity */
        @media (max-width: 1024px) {

            /* Responsive Table Logic */
            .responsive-table,
            .responsive-table thead,
            .responsive-table tbody,
            .responsive-table th,
            .responsive-table td,
            .responsive-table tr {
                display: block;
                width: 100%;
            }

            .responsive-table thead tr {
                position: absolute;
                top: -9999px;
                left: -9999px;
            }

            .responsive-table tr {
                background: white;
                border: 1px solid #e2e8f0;
                border-radius: 12px;
                margin-bottom: 16px;
                padding: 16px;
                box-shadow: 0 1px 2px rgba(0, 0, 0, 0.05);
            }

            .responsive-table td {
                border: none;
                padding: 10px 0;
                position: relative;
                display: flex;
                justify-content: space-between;
                align-items: flex-start;
                text-align: right;
                border-bottom: 1px solid #f8fafc;
            }

            .responsive-table td:last-child {
                border-bottom: none;
                margin-top: 12px;
                padding-top: 12px;
                border-top: 1px solid #f1f5f9;
                justify-content: flex-end;
            }

            .responsive-table td:before {
                content: attr(data-label);
                font-weight: 600;
                color: #64748b;
                font-size: 0.875rem;
                margin-right: 16px;
                text-align: left;
                width: 40%;
                flex-shrink: 0;
            }

            .responsive-table .driver-info {
                width: 60%;
                flex-direction: row-reverse;
                justify-content: flex-start;
            }

            .responsive-table .actions-cell {
                width: 100%;
                justify-content: flex-end;
                gap: 8px;
                flex-wrap: wrap;
            }

            .responsive-table td:last-child:before {
                display: none;
            }
        }
    </style>
</head>

<body>
    <div class="dashboard-app dashboard-app--minimal">
        <%- include("partials/sidebar", { active: 'withdrawals' }) %>

            <main class="dashboard-main dashboard-main--minimal">
                <%- include("partials/mobile-nav", { title: "Withdrawals" , user }) %>

                    <section class="page-header">
                        <div>
                            <p class="breadcrumbs">Start > Withdrawals</p>
                            <h1>Withdrawal Requests</h1>
                        </div>
                        <div class="header-actions">
                            <span class="timestamp">
                                <%= new Date().toLocaleDateString('en-US', { weekday: 'long' , year: 'numeric' ,
                                    month: 'long' , day: 'numeric' }) %>
                            </span>
                        </div>
                    </section>

                    <div class="content-wrapper" style="padding: 0;">
                        <% if (message) { %>
                            <div class="alert alert-<%= type === 'error' ? 'danger' : 'success' %>"
                                style="margin-bottom: 24px;">
                                <%= message %>
                            </div>
                            <% } %>

                                <!-- Statistics -->
                                <div class="stats-grid">
                                    <div class="stat-card">
                                        <div class="stat-label">Total Requested</div>
                                        <div class="stat-value">
                                            <%= new Intl.NumberFormat('en-US', { style: 'currency' , currency: 'QAR'
                                                }).format(stats?.totalAmountRequested || 0) %>
                                        </div>
                                        <div class="stat-subtext">
                                            <%= stats?.total || 0 %> total requests
                                        </div>
                                    </div>
                                </div>

                                <!-- Filters -->
                                <div class="filters-section">
                                    <form action="/withdrawals" method="GET" class="search-form">
                                        <div class="search-box">
                                            <svg width="20" height="20" viewBox="0 0 24 24" fill="none"
                                                stroke="currentColor" stroke-width="2">
                                                <circle cx="11" cy="11" r="8"></circle>
                                                <line x1="21" y1="21" x2="16.65" y2="16.65"></line>
                                            </svg>
                                            <input type="text" name="search"
                                                placeholder="Search by driver name or phone..." value="<%= search %>">
                                            <input type="hidden" name="status" value="<%= status %>">
                                        </div>
                                    </form>
                                </div>

                                <div class="status-tabs">
                                    <a href="/withdrawals?status=&search=<%= search %>"
                                        class="status-tab <%= !status ? 'active' : '' %>">All Requests</a>
                                    <a href="/withdrawals?status=pending&search=<%= search %>"
                                        class="status-tab <%= status === 'pending' ? 'active' : '' %>">Pending</a>
                                    <a href="/withdrawals?status=processing&search=<%= search %>"
                                        class="status-tab <%= status === 'processing' ? 'active' : '' %>">Processing</a>
                                    <a href="/withdrawals?status=completed&search=<%= search %>"
                                        class="status-tab <%= status === 'completed' ? 'active' : '' %>">Completed</a>
                                    <a href="/withdrawals?status=rejected&search=<%= search %>"
                                        class="status-tab <%= status === 'rejected' ? 'active' : '' %>">Rejected</a>
                                </div>

                                <div class="table-wrapper">
                                    <table class="responsive-table">
                                        <thead>
                                            <tr>
                                                <th>Driver</th>
                                                <th>Amount</th>
                                                <th>Bank Details</th>
                                                <th>Status</th>
                                                <th>Requested</th>
                                                <th>Actions</th>
                                            </tr>
                                        </thead>
                                        <tbody>
                                            <% if (withdrawals.length===0) { %>
                                                <tr>
                                                    <td colspan="6" class="empty-state">
                                                        <div class="empty-content">
                                                            <p>No withdrawal requests found</p>
                                                        </div>
                                                    </td>
                                                </tr>
                                                <% } else { %>
                                                    <% withdrawals.forEach(withdrawal=> { %>
                                                        <tr>
                                                            <td data-label="Driver">
                                                                <div class="driver-info">
                                                                    <div>
                                                                        <div class="fw-bold">
                                                                            <%= withdrawal.driverId?.userId?.profile?.firstName
                                                                                || 'Unknown Driver' %>
                                                                        </div>
                                                                        <div class="text-muted small">
                                                                            <%= withdrawal.driverId?.userId?.phoneNumber
                                                                                || 'No Phone' %>
                                                                        </div>
                                                                    </div>
                                                                </div>
                                                            </td>
                                                            <td data-label="Amount">
                                                                <div class="fw-bold">
                                                                    <%= new Intl.NumberFormat('en-US', {
                                                                        style: 'currency' , currency: 'QAR'
                                                                        }).format(withdrawal.amount) %>
                                                                </div>
                                                                <div class="text-muted small">Final: <%= new
                                                                        Intl.NumberFormat('en-US', { style: 'currency' ,
                                                                        currency: 'QAR'
                                                                        }).format(withdrawal.finalAmount) %>
                                                                </div>
                                                            </td>
                                                            <td data-label="Bank Details">
                                                                <% const bank=withdrawal.bankDetails ||
                                                                    withdrawal.driverId?.bankDetails; %>
                                                                    <% if (bank) { %>
                                                                        <div class="small">
                                                                            <div class="fw-bold">
                                                                                <%= bank.bankName || 'N/A' %>
                                                                            </div>
                                                                            <div class="text-muted">
                                                                                <%= bank.accountNumber || 'N/A' %>
                                                                            </div>
                                                                            <% if (bank.swiftCode) { %>
                                                                                <div class="text-muted small">SWIFT: <%=
                                                                                        bank.swiftCode %>
                                                                                </div>
                                                                                <% } %>
                                                                                    <% if (bank.bankAddress) { %>
                                                                                        <div class="text-muted small">
                                                                                            <%= bank.bankAddress %>
                                                                                                <%= bank.bankCity ? `,
                                                                                                    ${bank.bankCity}`
                                                                                                    : '' %>
                                                                                        </div>
                                                                                        <% } %>
                                                                        </div>
                                                                        <% } else { %>
                                                                            <span class="text-muted">No details</span>
                                                                            <% } %>
                                                            </td>
                                                            <td data-label="Status">
                                                                <span class="badge status-<%= withdrawal.status %>">
                                                                    <%= withdrawal.status.charAt(0).toUpperCase() +
                                                                        withdrawal.status.slice(1) %>
                                                                </span>
                                                            </td>
                                                            <td data-label="Requested">
                                                                <div class="text-muted small">
                                                                    <%= new
                                                                        Date(withdrawal.requestedAt).toLocaleDateString()
                                                                        %>
                                                                        <br>
                                                                        <%= new
                                                                            Date(withdrawal.requestedAt).toLocaleTimeString([],
                                                                            { hour: '2-digit' , minute: '2-digit' }) %>
                                                                </div>
                                                            </td>
                                                            <td>
                                                                <div class="actions-cell">
                                                                    <% if (withdrawal.status==='pending' ) { %>
                                                                        <form
                                                                            action="/withdrawals/<%= withdrawal._id %>/approve"
                                                                            method="POST" style="display:inline;">
                                                                            <button type="submit"
                                                                                class="action-btn btn-approve">Approve</button>
                                                                        </form>
                                                                        <button type="button"
                                                                            class="action-btn btn-reject"
                                                                            onclick="openRejectModal('<%= withdrawal._id %>')">Reject</button>
                                                                        <% } else if (withdrawal.status==='processing' )
                                                                            { %>
                                                                            <button type="button"
                                                                                class="action-btn btn-complete"
                                                                                onclick="openCompleteModal('<%= withdrawal._id %>')">Mark
                                                                                Paid</button>
                                                                            <button type="button"
                                                                                class="action-btn btn-reject"
                                                                                onclick="openRejectModal('<%= withdrawal._id %>')">Reject</button>
                                                                            <% } %>
                                                                </div>
                                                            </td>
                                                        </tr>
                                                        <% }) %>
                                                            <% } %>
                                        </tbody>
                                    </table>
                                </div>

                                <!-- Pagination -->
                                <% if (pagination.pages> 1) { %>
                                    <div class="pagination">
                                        <% if (pagination.page> 1) { %>
                                            <a href="/withdrawals?page=<%= pagination.page - 1 %>&status=<%= status %>&search=<%= search %>"
                                                class="page-link">Previous</a>
                                            <% } %>

                                                <span class="page-info">Page <%= pagination.page %> of <%=
                                                            pagination.pages %></span>

                                                <% if (pagination.page < pagination.pages) { %>
                                                    <a href="/withdrawals?page=<%= pagination.page + 1 %>&status=<%= status %>&search=<%= search %>"
                                                        class="page-link">Next</a>
                                                    <% } %>
                                    </div>
                                    <% } %>
                    </div>
            </main>
    </div>

    <!-- Modals -->
    <div class="modal-overlay" id="rejectModal">
        <div class="modal">
            <h3 class="modal-title">Reject Withdrawal</h3>
            <form id="rejectForm" method="POST">
                <div class="form-group">
                    <label style="display:block; margin-bottom: 8px;">Reason for rejection</label>
                    <textarea name="rejectionReason" class="form-control" required></textarea>
                </div>
                <div class="modal-actions">
                    <button type="button" class="action-btn" onclick="closeRejectModal()"
                        style="background: #f1f5f9; color: #64748b;">Cancel</button>
                    <button type="submit" class="action-btn btn-reject">Reject Request</button>
                </div>
            </form>
        </div>
    </div>

    <div class="modal-overlay" id="completeModal">
        <div class="modal">
            <h3 class="modal-title">Complete Withdrawal</h3>
            <form id="completeForm" method="POST">
                <p style="margin-bottom: 16px; color: #64748b;">Mark this withdrawal as paid? You can optionally add
                    transaction notes.</p>
                <div class="form-group">
                    <label style="display:block; margin-bottom: 8px;">Transaction Notes (Optional)</label>
                    <textarea name="notes" class="form-control" placeholder="e.g. Transaction ID: 123456"></textarea>
                </div>
                <div class="modal-actions">
                    <button type="button" class="action-btn" onclick="closeCompleteModal()"
                        style="background: #f1f5f9; color: #64748b;">Cancel</button>
                    <button type="submit" class="action-btn btn-complete">Confirm Payment</button>
                </div>
            </form>
        </div>
    </div>

    <script>
        function openRejectModal(id) {
            const modal = document.getElementById('rejectModal');
            const form = document.getElementById('rejectForm');
            form.action = `/withdrawals/${id}/reject`;
            modal.classList.add('active');
        }

        function closeRejectModal() {
            document.getElementById('rejectModal').classList.remove('active');
        }

        function openCompleteModal(id) {
            const modal = document.getElementById('completeModal');
            const form = document.getElementById('completeForm');
            form.action = `/withdrawals/${id}/complete`;
            modal.classList.add('active');
        }

        function closeCompleteModal() {
            document.getElementById('completeModal').classList.remove('active');
        }

        // Close on outside click
        window.onclick = function (event) {
            if (event.target.classList.contains('modal-overlay')) {
                event.target.classList.remove('active');
            }
        }
    </script>
    <script src="/js/dashboard.js"></script>
</body>

</html>